import { createTheme } from 'shadcn';

const theme = createTheme({
  colors: {
    primary: '#0070f3',
    secondary: '#1c1c1e',
    // Add more colors as needed
  },
  // Add more theme settings as needed
});

export default theme;